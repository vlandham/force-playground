import { createAction } from 'redux-actions';

export const addForce = createAction('ADD_FORCE');
export const removeForce = createAction('REMOVE_FORCE');
export const updateForce = createAction('UPDATE_FORCE');
